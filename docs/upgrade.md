# Upgrade Guide

## v1.0.0

First stable release. No upgrade steps required.

Future versions will document migration steps here. Each major release will list:

- Breaking changes to the `SpotlightSource` / `SpotlightResult` / `Handler` contracts
- New migrations and the order to run them
- Configuration keys renamed or removed
- Any required code changes in `AppPanelProvider`
